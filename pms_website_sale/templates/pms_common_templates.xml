<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <template id="progress_bar">
        <!--
            set the step value when calling the template to highlight the
            correct step in the progress bar :
            - step == -1 : Booking failed
            - step == 1  : Review Booking
            - step == 5  : Address
            - step == 10 : Confirm Booking
            - step == 20 : Booking success
        -->
        <div class="col-xl">
            <div class="wizard">
                <div class="progress-wizard">
                    <a class="no-decoration" href="#">
                        <div
                            id="wizard-review"
                            t-att-class="'progress-wizard-step ' + ('inactive' if step &lt; 1 else ('active' if step == 1 else 'complete'))"
                        >
                            <div class="progress-wizard-bar d-none d-md-block" />
                            <span
                                class="progress-wizard-dot d-none d-md-inline-block"
                            />
                            <div
                                class="text-center progress-wizard-steplabel"
                            >Review Booking
                            </div>
                        </div>
                    </a>
                    <a class="no-decoration" href="#">
                        <div
                            id="wizard-address"
                            t-att-class="'progress-wizard-step ' + ('inactive' if step &lt; 5 else ('active' if step == 5 else 'complete'))"
                        >
                            <div class="progress-wizard-bar d-none d-md-block" />
                            <span
                                class="progress-wizard-dot d-none d-md-inline-block"
                            />
                            <div class="text-center progress-wizard-steplabel">Address
                            </div>
                        </div>
                    </a>
                    <a class="no-decoration" href="#">
                        <div
                            id="wizard-confirm"
                            t-att-class="'progress-wizard-step ' + ('inactive' if step &lt; 10 else ('active' if step == 10 else 'complete'))"
                        >
                            <div class="progress-wizard-bar d-none d-md-block" />
                            <span
                                class="progress-wizard-dot d-none d-md-inline-block"
                            />
                            <div
                                class="text-center progress-wizard-steplabel"
                            >Confirm Booking
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </template>

    <template id="pms_booking_engine_summary">
        <div class="row">
            <div class="col">
                <div name="total">
                    <h2>Total</h2>
                    <span
                        t-esc="booking_engine.total_price_folio"
                        t-options='{"widget": "monetary", "display_currency": website.currency_id}'
                    />
                </div>
                <div>
                    <t t-if="booking_engine">
                        <a class="btn btn-primary" href="/booking/address">Next</a>
                        <a
                            class="btn btn-secondary"
                            t-attf-href="/rooms?start_date={{ booking_engine.start_date }}&amp;end_date={{ booking_engine.end_date }}"
                        >
                                Add other rooms
                        </a>
                    </t>
                    <t t-else="">
                        <a class="btn btn-secondary" href="/rooms">Add other rooms</a>
                    </t>
                </div>
            </div>
        </div>
    </template>

</odoo>
