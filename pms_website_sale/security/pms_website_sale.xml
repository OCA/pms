<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <record id="pms_room_type_public" model="ir.rule">
        <field name="name">Room type public</field>
        <field name="model_id" ref="pms.model_pms_room_type"/>
        <field name="domain_force">[('website_published', '=', True)]</field>
        <field name="groups" eval="[(4, ref('base.group_public')), (4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- This record exists because website_sale's security record also check
         against ('sale_ok', '=', True). We do not set sale_ok to True on room
         types (which are products), so we need a separate record.
    -->
    <record id="product_template_public" model="ir.rule">
        <field name="name">Public product template : pms</field>
        <field name="model_id" ref="product.model_product_template"/>
        <field name="domain_force">[('website_published', '=', True)]</field>
        <field name="groups" eval="[(4, ref('base.group_public')), (4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

</odoo>
